// Root page — delegates to the (public) landing page
// The (public) route group shares this root layout but this file takes precedence.
// We export the same LandingPage component here.
export { default } from './(public)/page'
